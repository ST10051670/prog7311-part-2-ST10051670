@model IEnumerable<dynamic>

@{
    ViewData["Title"] = "Farmer Details";
}

<h1>Farmer Details</h1>

<a href="@Url.Action("AddFarmer", "Employee")" class="btn btn-primary mb-3">Add Farmer</a>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <tr>
                <th>User ID</th>
                <th>Full Name</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var farmer in Model)
            {
                <tr>
                    <td>@farmer.UserId</td>
                    <td>@farmer.FullName</td>
                    <td>@farmer.Email</td>
                </tr>
            }
        </tbody>
    </table>
</div>

//W3Schools (2019)
//HTML tables. [online] W3schools.com
//Available at:https://www.w3schools.com/html/html_tables.asp. (Accessed: 5 May 2025).‌
